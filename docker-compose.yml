services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PORT=${PORT:-8000}
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
